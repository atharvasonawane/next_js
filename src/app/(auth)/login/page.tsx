// This comes into logicaal grouping of routes because login comes into (auth) folder and hence /login can be hit directly
// ie. directly use /login to go to login page
// and no need to do /auth/login or etc


const Login = () => {
    return <h1>Login</h1>;
}
 
export default Login; 